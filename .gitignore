# Dependencies
node_modules/

# E2E test artifacts
tests/e2e/.cache/

# Generated test fixtures (rebuilt by their respective test scripts on each run)
tests/fixtures/complexity/
tests/fixtures/community-scanner/

# Claude Code local state
.claude/plans/
.claude/hooks/

# Review artifacts (local Codex/cross-model reviews)
.reviews/

# Editor/OS
.DS_Store
*.swp
.claude/settings.local.json

# Session-local / tool cruft (prevents accidental `git add -A` sweeps)
# Anchored to repo root so future legitimate assets at nested paths are not hidden.
/.playwright-mcp/
/anthropic-sonnet-*.png
/awesome-claude-code-subagents/
scripts/__pycache__/
/~/

# tests/e2e/score-history.txt is recreated by test-cusum.sh on each run
# (test artifact; canonical store is score-history.jsonl)
tests/e2e/score-history.txt
