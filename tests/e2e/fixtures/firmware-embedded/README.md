# Firmware Overlay Manager

SD card overlay system for handheld gaming devices. Supports multiple hardware variants via device-specific config files.

## Build

```bash
make build DEVICE=device-a
make flash DEVICE=device-a
```
